#include <stdio.h>

/**
 * main - print a messange to display
 * Return: 0 if success, other case distinct 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
