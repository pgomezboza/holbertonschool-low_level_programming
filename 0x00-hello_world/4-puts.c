#include <stdio.h>
/**
 * main -  print a message to display
 * Return: 0 on success, otherwise not 0
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
