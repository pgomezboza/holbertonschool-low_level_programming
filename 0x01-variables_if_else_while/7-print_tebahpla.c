#include <stdio.h>
/**
 * main - a main function
 * Description: print the lowercase alphabet in reverse order.
 * Return: default 0
 */
int main(void)
{
	char ch = 'z';

	while (ch >= 'a')
	{
		putchar(c);
		ch--;
	}
	putchar('\n');

	return (0);
}
